name: publish-nuget

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: nuget-publish

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: mcp/dotnet-pilot-mcp-roslyn/global.json

      - name: Restore
        run: dotnet restore mcp/dotnet-pilot-mcp-roslyn/tests/DotnetPilot.Mcp.Roslyn.Tests/DotnetPilot.Mcp.Roslyn.Tests.csproj

      - name: Build
        run: dotnet build mcp/dotnet-pilot-mcp-roslyn/tests/DotnetPilot.Mcp.Roslyn.Tests/DotnetPilot.Mcp.Roslyn.Tests.csproj -c Release --no-restore

      - name: Test
        run: dotnet test mcp/dotnet-pilot-mcp-roslyn/tests/DotnetPilot.Mcp.Roslyn.Tests/DotnetPilot.Mcp.Roslyn.Tests.csproj -c Release --no-build --logger "trx;LogFileName=test-results.trx"

      - name: Pack
        run: dotnet pack mcp/dotnet-pilot-mcp-roslyn/src/DotnetPilot.Mcp.Roslyn/DotnetPilot.Mcp.Roslyn.csproj -c Release --no-build --no-restore

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: mcp/dotnet-pilot-mcp-roslyn/artifacts/packages/*
          if-no-files-found: error

      - name: Push NuGet package
        run: dotnet nuget push mcp/dotnet-pilot-mcp-roslyn/artifacts/packages/*.nupkg --api-key "${{ secrets.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Push symbol package
        run: dotnet nuget push mcp/dotnet-pilot-mcp-roslyn/artifacts/packages/*.snupkg --api-key "${{ secrets.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate
